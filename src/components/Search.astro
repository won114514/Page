---
// 搜索组件
---

<div class="search-container">
  <div class="search-input-wrapper">
    <svg class="search-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <circle cx="11" cy="11" r="8"></circle>
      <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
    </svg>
    <input
      type="text"
      id="search-input"
      placeholder="搜索文章... (按 / 键聚焦，回车搜索)"
      class="search-input"
    />
  </div>
</div>

<script>
  // 初始化搜索组件
  function initSearch(): void {
    const searchInput = document.getElementById('search-input') as HTMLInputElement | null;
    if (!searchInput) return;
    
    // 按 / 键聚焦搜索框
    document.addEventListener('keydown', (e) => {
      if (e.key === '/' && e.target instanceof Element && e.target.tagName !== 'INPUT' && e.target.tagName !== 'TEXTAREA') {
        e.preventDefault();
        searchInput.focus();
      }
    });
    
    // 按 ESC 键清空搜索
    searchInput.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        searchInput.value = '';
        searchInput.blur();
      }
    });
    
    // 初始化阿里云AI验证码
    let captcha: AliyunCaptcha;
    if (window.initAliyunCaptcha) {
      captcha = window.initAliyunCaptcha({
        SceneId: "xztyx6ag",  // 场景ID
        mode: "popup",  // 验证码模式，popup表示弹出式
        element: "#search-input",  // 页面上预留的渲染验证码的元素
        button: "#search-input",  // 触发验证码弹窗或无痕验证的元素ID
        // 验证码验证通过回调函数
        success: function (captchaVerifyParam) {
          // 验证通过后执行搜索
          const searchQuery = searchInput.value.trim();
          if (searchQuery) {
            window.location.href = `/search?q=${encodeURIComponent(searchQuery)}`;
          }
        },
        // 验证码验证失败回调函数
        fail: function (errorCode) {
          console.error('验证码验证失败:', errorCode);
        },
        // 验证码渲染失败回调函数
        error: function (errorCode, errorMsg) {
          console.error('验证码渲染失败:', errorCode, errorMsg);
        }
      });
    }
    
    // 回车键处理 - 触发验证码验证
    searchInput.addEventListener('keydown', (e) => {
      if (e.key === 'Enter' && searchInput.value.trim()) {
        e.preventDefault();
        
        // 触发验证码验证
        if (captcha && captcha.show) {
          captcha.show();
        } else {
          // 如果验证码未初始化，直接执行搜索（降级处理）
          window.location.href = `/search?q=${encodeURIComponent(searchInput.value.trim())}`;
        }
      }
    });
  }
  
  // 页面加载完成后初始化
  if (typeof window !== 'undefined') {
    window.addEventListener('DOMContentLoaded', initSearch);
  }
</script>

<style>
  .search-container {
    position: relative;
    width: 200px;
    display: flex;
    align-items: center;
    z-index: 1000;
  }
  
  .search-input-wrapper {
    position: relative;
    width: 100%;
    display: flex;
    align-items: center;
  }
  
  .search-icon {
    position: absolute;
    left: 0.75rem;
    width: 16px;
    height: 16px;
    color: var(--text-color);
    opacity: 0.5;
    transition: all 0.3s ease;
    pointer-events: none;
    z-index: 2;
  }
  
  .search-input {
    width: 100%;
    padding: 0.5rem 1rem 0.5rem 2.5rem;
    border: 1px solid var(--border-color);
    border-radius: 24px;
    font-size: 0.9rem;
    background-color: var(--bg-color);
    color: var(--text-color);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    font-family: var(--font-sans);
    box-sizing: border-box;
    z-index: 1;
  }
  
  .search-input::placeholder {
    color: var(--text-color);
    opacity: 0.5;
    transition: opacity 0.3s ease;
  }
  
  .search-input:focus {
    outline: none;
    border-color: var(--accent-color);
    box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.05);
    width: 300px;
    background-color: var(--bg-color);
  }
  
  [data-theme="dark"] .search-input:focus {
    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.1);
  }
  
  .search-input:focus + .search-icon,
  .search-input:hover + .search-icon {
    opacity: 0.7;
  }
  
  /* 响应式设计 */
  @media (max-width: 768px) {
    .search-container {
      width: 180px;
    }
    
    .search-input:focus {
      width: 280px;
    }
  }
  
  @media (max-width: 480px) {
    .search-container {
      width: 150px;
    }
    
    .search-input {
      font-size: 0.85rem;
      padding: 0.45rem 0.8rem 0.45rem 2.25rem;
    }
    
    .search-input:focus {
      width: 220px;
    }
  }
</style>
